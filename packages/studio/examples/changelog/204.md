# Layer 204

Authored notes for this layer (Markdown) — they render above the auto-diff. Use
this for the *why*: migration guidance, breaking changes, deprecations. The
added / changed / removed lists below are generated automatically from the schema.

## Highlights

- **New:** `crypto.updateNewBalance` — pushed when a wallet balance changes; carries
  `balance` + `pts`/`pts_count` so clients can keep their update state in sync.
- No breaking changes for existing clients on layer 203.

> Tip: bundle these with `mt-tl-studio build --changelog <dir>` (files named
> `<layer>.md`).
