# Send & receive

A prose guide describing a messaging flow: one user sends, another receives the
pushed update. Group related guides under folders (this one lives in
`messaging/`) — the sidebar renders them as a tree.

Use **▸ listen updates** to watch the pushes live, or build a two-user scenario
in **▸ new scenario** with an `expectUpdate` step.
